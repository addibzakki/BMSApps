const config = {
  screen: {
    WorkArea: {
      path: 'WorkArea',
    },
    Profile: {
      path: 'Profile',
    },
  },
};

const linking = {
  prefixes: ['bms://app'],
  config,
};

export default linking;
